apiVersion: v1
kind: ConfigMap
metadata:
  name: worker-service-config
  namespace: default
data:
  NODE_ENV: "production"
  LOG_LEVEL: "info"
  WORKER_CONCURRENCY: "5"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker-service
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker-service
  template:
    metadata:
      labels:
        app: worker-service
    spec:
      containers:
      - name: worker-service
        image: YOUR_DOCKER_USERNAME/worker-service:latest
        imagePullPolicy: Always
        env:
        - name: NODE_ENV
          valueFrom:
            configMapKeyRef:
              name: worker-service-config
              key: NODE_ENV
        - name: MONGO_URI
          value: "mongodb://mongodb:27017/products"
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        - name: PRODUCT_SERVICE_URL
          value: "http://product-service:5000"
        - name: WORKER_CONCURRENCY
          valueFrom:
            configMapKeyRef:
              name: worker-service-config
              key: WORKER_CONCURRENCY
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: worker-service-config
              key: LOG_LEVEL
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: worker-service-hpa
  namespace: default
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: worker-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80

